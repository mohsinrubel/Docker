Learning Docker [Day 2] 🚀

✅ Let's learn about the Step-By-Step Docker Installation on Linux & Docker Basic Commands

To start the installation of Docker, we are going to use an Amazon Linux EC2 instance.
To install Docker, we need to follow the steps given below.

Step 1 − Log into your system as a user with sudo privileges. Before installing Docker, you first have to ensure that you have the right Linux kernel version running. Docker is only designed to run on Linux kernel version 3.8 and higher. We can do this by running the following command.

Step 2 − uname: This method returns the system information about the Linux system − uname -a

Step 3 − You need to update the OS with the latest packages, which can be done via the following command − sudo yum update -y.

Step 4 − The next step is to install the necessary certificates that will be required to work with the Docker site later on to download the necessary Docker packages. It can be done with the following command − sudo apt-get install apt-transport-https ca-certificates

Step 5 − The final step is to install Docker and we can do this with the following command − sudo yum install docker –y
Here, yum uses the install option to download the Docker image from the Docker website and get Docker installed.

Step 6 − Docker Version
To see the version of Docker running, you can issue the following command − sudo docker version

Step 7 − Docker Info
To see more information on the Docker running on the system, you can issue the following command − sudo docker info

Step 8 − Status Docker − sudo service docker status

Step 9 − Start Docker − sudo service docker start

Step 10 − Verify Docker − sudo docker run hello-world

✅ Docker Commands Cover

➡ docker run
Usage: docker run -it -d <image name>
This command is used to create a container from an image

➡ docker ps
This command is used to list the running containers

➡ docker ps -a
This command is used to show all the running and exited containers

➡ docker images
This command lists all the locally stored docker images

Note_1: Verification of pulling an image from the docker hub and creating a container from this image on the Docker Desktop cover at the start of this video then I start how to install Docker on Linux-based OS. Because the last video on how to install Docker desktop on windows' last steps of verification is not added due to 15 min video length limit.

Note_2: You can install Docker Engine in different ways, depending on your needs so if you have more details about the installation of Docker then visit this link for a better understanding: https://lnkd.in/dnWUSWHa
